# Pass-Rusher Gravity GNN — Python dependencies
# Pinned to versions known to work together for PyTorch Geometric on Python 3.10/3.11.
# If you are on Apple Silicon or CUDA, use the install commands documented in the
# branch README; pip alone may not pull the correct wheels for torch_scatter / torch_sparse.

# Core scientific stack
numpy>=1.24,<2.1
pandas>=2.0,<2.3
scipy>=1.10
matplotlib>=3.7
seaborn>=0.13
scikit-learn>=1.3

# Notebook / display
jupyterlab>=4.0
ipykernel>=6.25
ipywidgets>=8.0
tqdm>=4.66

# Deep learning core (install matching CUDA/CPU build per README)
torch>=2.1,<2.4

# PyTorch Geometric stack
torch-geometric>=2.4,<2.6
# Optional native ops; only needed for some message-passing layers / sparse ops.
# Install with the right wheel index URL — see README.
# torch-scatter>=2.1
# torch-sparse>=0.6
# torch-cluster>=1.6

# Optional gradient boosting baseline (alternative to sklearn HistGradientBoosting)
xgboost>=2.0
lightgbm>=4.1
